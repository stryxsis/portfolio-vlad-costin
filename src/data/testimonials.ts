/**
 * Testimonianze.
 *
 * VUOTO di proposito: non ce ne sono di reali. Il componente e la sezione
 * esistono già e si accendono da `SITE.features.testimonials` in src/lib/site.ts
 * appena questo array ha una voce vera.
 *
 * Non ci sono placeholder "Mario Rossi — CEO": una testimonianza inventata su un
 * portfolio è l'unica cosa che, se scoperta, azzera la fiducia in tutto il
 * resto della pagina.
 */
import type { Testimonial } from '../lib/types';

export const TESTIMONIALS: Testimonial[] = [];