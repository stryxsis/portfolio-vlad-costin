# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty. No source code, configuration, or documentation exists yet.

Once the project is scaffolded (framework, build tooling, tests, etc.), update this file with:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture and structure notes that aren't obvious from browsing individual files

## Git workflow

This repo is tracked on GitHub (`portfolio-vlad-costin`, public). The user has pre-authorized the following without asking for confirmation each time:
- Commit whenever a meaningful, working chunk of progress is reached during a session (not after every tiny edit).
- Push to the remote periodically as part of that same workflow.

Still avoid destructive git operations (force-push, reset --hard, history rewrites) without explicit confirmation.
